import 'package:flutter/material.dart';

class TemplateWPage extends StatelessWidget {
  const TemplateWPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
